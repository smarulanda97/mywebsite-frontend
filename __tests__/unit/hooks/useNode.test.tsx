/**
 * @todo Waiting for version of package that supports react 18
 */
// import { renderHook } from "@testing-library/react-hooks";

import { useNode } from "@/hooks";
import { render, Wrapper } from "@/lib";

describe("useNode()", function () {
  test("Should return node data", async function () {
    // const { result, waitForNextUpdate } = renderHook(() => useNode(), { wrapper: Wrapper });
    expect(undefined).toBeUndefined();
    // await waitForNextUpdate();
  });
});
